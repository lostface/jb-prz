module.exports = strDateComparator;

function strDateComparator(strDate1, strDate2) {
  const
    time1 = new Date(strDate1).getTime(),
    time2 = new Date(strDate2).getTime();

  return time1 < time2 ? -1 : (time1 == time2 ? 0 : 1);
}
