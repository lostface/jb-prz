'use strict';

const
  test = require('tape'),
  constTrue = require('./index.js');

test('constTrue return value', function(t) {
  const expected = true;

  const actual = constTrue();

  t.equal(actual, expected, 'constTrue() should return true');

  t.end();
});
