'use strict';

const deepEqual = require('deep_equal');

module.exports = shallowEqual;

function shallowEqual(thing1, thing2, strict) {
  return deepEqual(thing1, thing2, { strict, shallow: true });
}
