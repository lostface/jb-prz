const
  test = require('tape'),
  const0 = require('./index.js');

test('const0 return value', function(t) {
  const expected = 0;

  const actual = const0();

  t.equal(actual, expected, 'const0() should return 0');

  t.end();
});
