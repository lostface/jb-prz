{
  "name": "backend",
  "version": "0.0.1",
  "main": "server.js",
  "scripts": {
    "test": "tape **/*.test*.js | tap-spec"
  },
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0"
  }
}
